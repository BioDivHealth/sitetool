globalVariables(unique(c(
  # calculatePatchMetrics:
  "layer",
  # createLCDataFrame:
  "area", "cover", "cover_total_area", "layer", "mean_patch_area", "proportion", "site_id", "total_area",
  # generate_plot:
  "color", "group", "input_site", "site", "site_id", "value",
  # generate_text:
  "input_site",
  # get_cities:
  "ciies",
  # get_land_area:
  "lakes_boundaries", "land_boundaries",
  # get_village_points:
  "geometry", "name", "osm_id", "population", "site",
  # mod_step1_server : <anonymous>:
  "product", "raster_cats", "subcover", "value",
  # mod_step2_server : <anonymous>:
  "input_site", "site", "site_id",
  # mod_step2_server : <anonymous> : <anonymous>:
  ".",
  # mod_step3_server : <anonymous>:
  ".", "color", "cover", "group", "input_site", "latitude", "longitude", "measure", "raster_cats", "site", "site_id", "subcover", "value",
  # mod_step3_server : <anonymous> : <anonymous>:
  "cover", "measure"
)))
